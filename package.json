{
  "name": "negative-keyword-wrapper",
  "version": "1.0.0",
  "description": "Web-based tool to wrap Google Ads negative keywords",
  "scripts": {
    "deploy": "wrangler deploy --assets .",
    "deploy:preview": "wrangler versions upload --assets ."
  }
}
